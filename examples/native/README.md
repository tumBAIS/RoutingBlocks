# Native RoutingBlocks Extension

This example shows how to extend RoutingBlocks with native code, i.e., to implement an efficient evaluation procedure
for a specific problem setting.

## Building

- Depends on pybind11

## Usage
def test_station_removal_search():
    # Create a feasible solution that has a ton of charging stations but would be feasible without any
    # Should remove all of them

    # Create a solution that needs a charging station but has a ton of them
    # Should remove all of them but one
    pass